input {
  # tcp { #ncat
  #   port => 5000
  #   codec => "json_lines"
  # }
  # gelf { #uses udp dockerd way
  #   port => 12201
  # }

  tcp {
    port => 5001
    codec => "json"
  }
}

filter { }

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logstash-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}