{
  "name": "pong-online",
  "version": "1.0.0",
  "description": "Online version of Pong",
  "type": "module",
  "scripts": {
    "build:client": "cd client && tsc && vite build",
    "build:server": "cd server && tsc",
    "build": "npm run build:client && npm run build:server"
  },
  "dependencies": {
    "pong-shared": "file:../Shared",
    "fastify": "^5.6.2",
    "fastify-metrics": "^12.1.0",
	"@fastify/jwt": "^10.0.0",
    "node-vault": "^0.10.9",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
